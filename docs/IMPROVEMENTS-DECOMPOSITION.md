# Пакет G — улучшения по итогам разбора: пользователь и владелец продукта

> Разбор после фазы F (проверено по коду, а не по памятке) дал десять пунктов: четыре со
> стороны пользователя, шесть со стороны владельца. Здесь — план их выполнения: порядок,
> миграции, решения и то, чего пакет **не** делает. Разбор нашёл по дороге ещё одну дыру
> (G1), и она идёт первой.
>
> Правила дома действуют целиком: пробел называется, а не заполняется; оговорка едет с
> числом; одна дверь на правило; второй правды не заводим; каждый новый страж проверяется
> снятием. Методика меняется **только** в G10 и G11 — каждый со своим бампом
> `ENGINE_VERSION` и осознанным ревью golden-диффа.

## Сквозные решения

1. **Решения владельца не принимаются кодом.** Включить почту и сбор событий — решение
   того, кто держит установку. Пакет умолчаний не переключает; он делает выключенное
   **видимым** (G9) и называет, что из-за него не работает.
2. **Планировщик — Celery beat.** Celery и Redis в проекте уже есть; второй механизм
   фоновых задач был бы второй правдой о том, что и когда запускается. Каждая задача —
   тонкая обёртка над функцией приложения, которую зовёт и CLI (одна дверь).
3. **Деньги клиента не списываются без его явного согласия.** Автопродление включается
   только отдельной отметкой при оплате, выключается в любой момент, и о каждом списании
   клиент узнаёт **до** него.
4. **Документы продавца переживают покупателя.** Счета и акты — первичные документы
   платформы как продавца (ст. 29 402-ФЗ): удаление организации-клиента их не стирает.

---

## G1 — ручной провайдер не принимает «оплату» в продакшене (починка)

**Что сломано.** `billing._build_provider()` выбирает `ManualPaymentProvider`, когда не
заданы ключи ЮKassa, **при любом `APP_ENV`**. Ручной провайдер включает тариф сразу и без
денег. В продакшене без ключей любой владелец организации берёт себе платный тариф одним
запросом `checkout` — та же дыра, что закрыл F1, только через другую дверь.

- [x] В `production` без ключей выбирается `UnavailablePaymentProvider`: он **отказывает**
      (503) с названной причиной и называет выход — оплату по счёту через платформу.
      Fail-closed, как у `JWT_SECRET`. Половина ключей — тоже отказ: одна переменная из
      двух это забытая настройка, а не подключённая оплата.
- [x] Вне `production` поведение прежнее: разработке и тестам ручной провайдер нужен.
- [x] Тест проверяет, что **ничего не случилось**, а не только код ответа: тариф прежний,
      платёж не заведён, журнал молчит (`tests/test_payment_provider_choice.py`, 9).
- [x] **Найдено по дороге:** экран тарифа показывал всё, кроме 403, как «Не удалось сменить
      тариф» — названная причина и выход до клиента не доходили. Теперь тост печатает
      `detail` сервера; без него — прежний общий текст (2 теста, страж проверен снятием).
- [x] `provider_kind()` — какой провайдер принимает оплату; его прочтёт экран готовности
      (G9).

## G2 — защита от одновременной правки (пользователь, п. 1) ✅

**Что было сломано.** `PUT` проекта и дела перезаписывал модель целиком без проверки. Двое
открыли, оба сохранили — правки первого исчезали молча; в журнале две записи `update`.

- [x] **Ревизия — отпечаток имени и модели** (`edit_conflict.revision_of` через
      `crud.model_hash`, тот же, что у гейта финализации), **не `updated_at`**.
      `updated_at` отвечает «писали ли в строку», а не «менялось ли содержимое»:
      финализация пишет статус и снимок ревью, и onupdate сдвигает его при прежней модели.
      Версия по нему давала бы 409 тому, кто правит, пока коллега финализирует (тест
      проверяет и сам сдвиг `updated_at`, и неизменность ревизии). Сводку расчёта от того
      же сдвига уже защищал явный core-update — здесь об onupdate обжигались и раньше.
      **Без миграции.**
- [x] Устаревшая `expected_revision` → **409**, и отказ называет **кто и когда** (последняя
      запись `update`/`version_restore` в журнале): восстановленная версия названа своим
      именем, запись по ключу API — с именем ключа и его автором, без записи в журнале —
      «кто это сделал, журнал не называет». Глагол — в страдательном залоге (род автора
      неизвестен), а род **объекта** свой: «проект изменён, он сохранён» / «дело изменено,
      оно сохранено».
- [x] Совпадение содержимого конфликтом не считается — терять нечего.
- [x] Без `expected_revision` — прежняя перезапись (ключи и скрипты до G2); сказано в
      описании маршрута. Интерфейс присылает ревизию всегда, пустую — не шлёт вовсе.
- [x] Интерфейс — одна модалка на оба продукта (`components/EditConflict.tsx`):
      **безопасный выход первым и в одно нажатие** — «сохранить мои правки новым
      проектом/делом»; «открыть их версию» и «сохранить поверх» — **только вторым
      нажатием**, и каждое называет, чьё пропадёт. «Правки, сохранённые раньше вас», а не
      «правки коллеги»: перезаписать можно и собственную версию из соседней вкладки.
      «Поверх» берёт **свежую** ревизию, иначе сервер ответил бы тем же конфликтом.
- [x] **Попутно:** формат времени в сообщениях сервера переехал в `app/timefmt.py` (одна
      дверь; служебный роутер зовёт её же), и наивное время SQLite теперь объявляется UTC —
      `astimezone` принимало его за местное и сдвинуло бы на пояс машины сервера.

**Тесты:** `tests/test_edit_conflict.py` (12), `components/EditConflict.test.tsx` (9),
`api/projects.test.ts` (4). Стражи проверены снятием: выключенная проверка (4 падения),
ревизия по `updated_at` (падает тест финализации), род дела (1), двойное нажатие (3),
неотправленная ревизия (1).

## G3 — планировщик (владелец, п. 7) ✅

- [x] `celery_app.conf.beat_schedule` (часовой пояс назван: UTC) + сервис `beat` в
      `docker-compose.yml`; процесс beat — ровно один, это сказано рядом с ним.
- [x] Ядро сверки неоплаты переехало в `app/scheduler.py`; задача
      (`scheduler.expire_subscriptions`) и скрипт эксплуатации зовут **одну функцию**
      (`expire_overdue`), у скрипта своей копии нет — это проверяет тест по вызовам.
- [x] **Каждый** запуск оставляет след в служебном журнале (`scheduler.expire`), даже
      когда делать было нечего: «проверили — просроченных нет» тоже ответ, а молчание
      журнала должно означать «не запускался». След **подписан источником** — ручной
      запуск скрипта не выдаёт себя за работающий планировщик. Автора у записи нет: это
      платформа, а не человек. `last_runs()` отвечает G9; «ни разу» — `None`, не ноль.
- [x] Перечни в обе стороны: в расписании нет задачи, которую Celery не знает (опечатка
      молча выключила бы её навсегда), и нет задачи `scheduler.*` вне расписания.
- [x] Перечень дверей арендатора пополнился `scheduler.py` — с причиной.
- [x] **Попутно:** тексты оттока (F8) говорили «записи оставляет эксплуатация, а не
      приложение» — после G3 это неправда. Теперь они называют сверку неоплаты и оба её
      источника (планировщик или скрипт).

**Тесты:** `tests/test_scheduler.py` (11); тесты неоплаты переведены со скрипта на модуль.
Стражи проверены снятием: след только при изменениях (1 падение), опечатка в расписании
(2), скрипт без подписи источника (1).

## G4 — письма о деньгах (владелец, п. 6) ✅

- [x] Три письма: за 7 дней до конца периода, в день окончания (идёт льготный срок), при
      закрытии записи. Этап — чистая функция `billing_period.reminder_stage` над теми же
      правилами, что и доступ: письмо не скажет «закрыто», пока экран ещё пускает.
      «Не истекает» (бесплатный, «по запросу») писем не получает; `canceled` — тоже: это
      акт человека, о нём знают.
- [x] Каждое называет срок **числом** («через 7 дн.», округление вверх — «через 0 дн.»
      не срок), что будет дальше, **что останется** (просмотр, расчёт, выгрузка) и оба
      выхода: оплату и оплату по счёту. Ссылка ведёт **сразу на вкладку оплаты**
      (`/organization?tab=billing` — вкладку по адресу страница организации раньше не
      открывала); без `PUBLIC_URL` раздел назван словами, а не ссылкой в никуда.
- [x] Получатели — участники с `billing.manage`, и только действующие: ни
      приостановленный участник, ни заблокированная учётная запись.
- [x] Письма **дверные** — о доступе к своей организации, уходят и на неподтверждённый
      адрес. Перечень информационных писем не изменился.
- [x] Повтор не уходит: факт — запись `billing.reminder` в журнале организации с ключом
      «продукт + конец этого периода + этап»; новый период — новый ключ.
- [x] **Три несводимых состояния:** почта выключена — **не пытались** (журнал клиента
      молчит, и включённая позже почта отправит то же письмо); неудача — отдельное
      действие `billing.reminder_failed` с причиной, повтор **не гасится**; ушло — запись
      с числом адресатов.
- [x] Задача `scheduler.billing_reminders` в 06:00 UTC (09:00 МСК) — после ночной сверки
      и к началу рабочего дня; след запуска с итогом, включая «почта выключена — не
      отправлено N».
- [x] **Попутно:** письмо со ссылкой доступа говорило «{адрес} выдал вам ссылку» — род
      того, о ком известен только адрес. Теперь «ссылка … (от {адрес}, организация …)».

**Тесты:** `tests/test_billing_reminders.py` (19), `pages/OrganizationPage.test.tsx` (3).
Стражи проверены снятием: без сверки по журналу, без проверки почты, неудача как успех,
письмо всем участникам, письмо приостановленным — по одному падению на каждый.

## G5 — автопродление и годовая оплата (владелец, п. 5)

- [ ] Миграция: `subscriptions.auto_renew`, `payment_method_id`, `payment_method_title`,
      `renew_error`, `renew_attempted_at`; `payments.months`.
- [ ] Согласие — отдельной отметкой при оплате; ЮKassa получает `save_payment_method`.
      Способ сохраняется по вебхуку, только если провайдер подтвердил сохранение.
- [ ] Списание — задача планировщика за сутки до конца периода; письмо **до** списания,
      письмо об итоге; неудача → причина, не больше одной попытки в сутки, до трёх.
- [ ] Отключение в любой момент; сохранённый способ забывается; включить снова — только
      новой оплатой с согласием.
- [ ] **Найдено при проектировании:** досрочная оплата того же тарифа отсчитывает новый
      период **от даты платежа**, и клиент теряет оставшиеся дни. Продление идёт от конца
      текущего периода.
- [ ] Годовая оплата в самообслуживании (12 месяцев). Скидка — из окружения, по умолчанию
      ноль: размер скидки — решение владельца, а не кода.

## G6 — закрывающие документы (владелец, п. 8)

- [ ] Реквизиты продавца — из окружения (`SELLER_*`). Не заданы → документ **не
      формируется** и отказ это называет: бланк с пустыми реквизитами выглядел бы
      настоящим.
- [ ] Реквизиты покупателя — у организации (миграция); ИНН/КПП проверяются тем же кодом,
      что реквизиты дела (`audit_core/requisites.py`).
- [ ] Таблица `billing_documents` (счёт/акт, номер в пределах года, снимок реквизитов
      покупателя на дату документа). Переживает организацию (`KEPT_AFTER_ORGANIZATION`),
      RLS-политики нет по той же причине, что у платежей (`NO_RLS_POLICY` с причиной).
- [ ] Счёт на оплату — по запросу клиента (тариф × месяцы); акт — на каждый успешный
      платёж, датой окончания оплаченного периода; до этой даты — «акт будет сформирован …».
- [ ] DOCX + сумма прописью (своя функция с тестами на все формы: рубль/рубля/рублей,
      11–14, тысячи женского рода).
- [ ] **Счёт-фактура и УПД не формируются** — требуют ЭДО и учётной системы продавца; это
      сказано рядом со списком документов.

## G7 — трекер ошибок (владелец, п. 9)

- [ ] `sentry-sdk` (совместим с GlitchTip, который разворачивается у себя). Без `SENTRY_DSN`
      выключен и ничего не отправляет.
- [ ] Вычистка до отправки: тело запроса, cookies, заголовок авторизации, почта — в отчёт
      не уходят (152-ФЗ).
- [ ] Ошибки фронта — через свой маршрут: DSN не попадает в бандл, вычистка в одном месте;
      предел размера и частоты.

## G8 — зависшие задачи (владелец, п. 7)

- [ ] `STUCK_JOB_ALERT_MINUTES` — по умолчанию выключено: порог — решение эксплуатации
      (фаза F отказалась от рассылки «с порогами, которых неоткуда взять»; здесь порог
      задаёт человек).
- [ ] Письмо операторам, одно на задачу (след в служебном журнале).

## G9 — готовность установки (владелец, п. 10)

- [ ] Служебная вкладка + `scripts/check_readiness.py` из одной функции: почта, события,
      планировщик, трекер, платёжный провайдер, реквизиты продавца, публичный адрес.
- [ ] По каждому пункту: состояние, **что не работает**, чем включить.
- [ ] Умолчания не переключаются.

## G10 — ограничение переноса убытков (пользователь, п. 2; 0.9.44)

- [ ] `settings.loss_carryforward_limit` (0.5 по п. 2.1 ст. 283 НК РФ). Налоговый год —
      12 месяцев от старта проекта, как у периодичности уплаты (одна конвенция).
- [ ] Убыток **текущего** года гасит прибыль того же года полностью — это не перенос;
      убытки **прошлых** лет уменьшают базу не больше чем на долю.
- [ ] `limit = 1` ≡ прежние числа (equivalence-тест). Норма временная и продлевалась —
      это сказано в карте методики, параметр позволяет её снять.
- [ ] Ревью golden-диффа; расхождение уходит из карты методики.

## G11 — НДС с полученных авансов (пользователь, п. 2; 0.9.45)

- [ ] В режиме «по отгрузке» база — на **наиболее раннюю** из дат (п. 1 ст. 167 НК РФ):
      по каждой строке сбыта накопленное признание = максимум накопленного по отгрузке и
      по оплате. Разница паркуется в B7 (НДС с авансов полученных — актив).
- [ ] Модели без предоплаты — те же числа (equivalence-тест). Ревью golden-диффа.

## G12 — онбординг «Элита» (пользователь, п. 3)

- [ ] Путь: цель (новый бизнес / действующий / из дела «Аудита») → шаблон, горизонт,
      старт → имя. Оговорка шаблона «числа выдуманы» — **до** создания.
- [ ] Пустой список проектов ведёт сюда.

## G13 — импорт модели из Excel (пользователь, п. 3)

- [ ] Прямые и постоянные издержки (ряды по месяцам), персонал (оклад, численность, сроки).
- [ ] По имени: найденное обновляется, недостающее **создаётся** — в отчёте отдельно, с
      названными умолчаниями, которые стоит проверить. Продукты по-прежнему не создаются.

## G14 — бизнес-план из дела «Аудита» (пользователь, п. 4)

- [ ] Стартовый баланс из последнего периода дела; старт — после его окончания.
- [ ] Неразделимое не угадывается: «Краткосрочные обязательства» в форме аудита не делятся
      на займы и кредиторку — отнесены к краткосрочным займам (не гасятся автоматически),
      и это сказано; запасы — одной строкой.
- [ ] Происхождение — разделом плана (`business_plan`), чтобы уехало и в DOCX.

## G15 — матрица скриншотов P13

- [ ] Playwright: ключевые экраны × светлая/тёмная × 1240/864/402 → PNG + галерея.
- [ ] Отметить в P13 только то, что действительно просмотрено.

---

## Чего пакет не делает

- **Не переключает умолчания** почты и событий (сквозное решение 1).
- **Не формирует счёт-фактуру и УПД** (G6) — нужна учётная система продавца.
- **Не подтверждает методику** и не объявляет `1.0`: G10 и G11 закрывают два названных
  расхождения с нормой, но четыре суждения §22 остаются работой человека.
- **Не заводит файловое хранилище** — три вопроса из `OPEN-DECISIONS` не отвечены.

## Порядок и миграции

G1 (дыра) → G2 → G3 → G4 → G5 → G6 → G7 → G8 → G9 → G10 → G11 → G12 → G13 → G14 → G15.
G4, G5 и G8 опираются на G3; G9 собирает сделанное в G3–G8.

| Шаг | Миграция |
|---|---|
| G5 | подписки: автопродление; платежи: месяцы |
| G6 | реквизиты покупателя у организации; `billing_documents` |
| остальные | — |

## Проверка на каждом шаге

`cd backend && ruff check . && python -m mypy && pytest -q`;
`cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0 && npx vitest run`;
миграция вверх-вниз-вверх; каждый новый страж — снятием.

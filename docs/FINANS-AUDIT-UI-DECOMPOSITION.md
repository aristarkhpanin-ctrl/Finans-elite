# Переход «Финанс Аудит» на макеты хендоффа — декомпозиция

**Цель.** Интерфейс и функционал продукта приводятся к `docs/design/finans-audit/`
(22 экрана, стиль-гайд, печатный бланк). **Относительно текущего состояния не теряется
ничего.**

Читать вместе с `docs/design/finans-audit/СВЕРКА-с-текущим-кодом.md` (почему хендофф
описывает другой охват) и `docs/FINANS-AUDIT-DECOMPOSITION.md` (что уже построено).

---

## 0. Правило, которое держит «не потерять»

Обещание не является механизмом. Механизм — **существующий набор тестов**:

| | Сейчас | Правило |
|---|---|---|
| Бэкенд аудита | 106 тестов в 9 файлах + `golden_audit` | только прирастает |
| Фронт аудита | `auditBalance` · `auditExport` · `auditXlsx` | только прирастает |
| Платформа + Элит | 686 бэкенд / 261 фронт | не трогаются |

**Ни один тест не удаляется и не ослабляется.** Выпадет возможность — упадёт её тест.
Если экран макета требует убрать что-то работающее, это не «переделка по макету», а
изменение продукта: сначала решение в этом документе, потом код.

Второй механизм — таблица §3 ниже: у каждой сегодняшней возможности назван
экран-хозяин. Возможность без хозяина — незакрытый пункт, а не «мелочь».

## 1. Принятые решения

**Палитра — канон хендоффа.** `#C77DFF` заливка, `#7A2BC4` текстовый акцент светлой
темы (Deep Violet), отдельная палитра печати. Текущий `#BC13FE` был получен поворотом
оттенка зелёного, а не нарисован: у него нет решения для текста на светлом фоне
(`#C77DFF` даёт ~2.7:1 к белому — как текст не работает, поэтому хендофф и держит для
этого второй хекс). Берём хендофф как единственный источник: все 22 экрана уже
посчитаны в его значениях, и `renderVals()` остаётся эталоном без пересчёта.

**Порядок — фазы 0–3 (интерфейс), затем 4–6 (ядро due diligence).** Сначала токены,
каркас и перенос существующих экранов: интерфейс становится как на макетах, функционал
не трогается ни на йоту. Обратный порядок заставил бы строить экраны дважды — сперва
на старых токенах, потом на новых.

**`calc_core` и его golden не трогаются нигде.** «Финанс-Элит» уже соответствует своим
макетам (`modal-redesign`, P0–P13 закрыты).

**Хранилище не переименовывается.** Макеты говорят «дело» (№ A-2417), код —
«субъект анализа». Таблица `audit_subjects`, модель `AuditSubjectModel` и эндпоинты
`/api/v1/audit/subjects` **остаются как есть**; «дело» — подпись в интерфейсе.
Переименование слоя хранения ради подписи сломало бы ручное зеркало модели и API
ради нуля пользы (ровно тот молчаливый разрыв, против которого написан
`model.contract.test.ts`).

## 2. Что построено сегодня — полный инвентарь

### Модель субъекта (`audit_core/models.py`)
1. Реквизиты: имя, валюта, отрасль
2. **Основа отчётности** — `rsbu | ifrs | management` (признак, не трансформация)
3. **Периоды** — подпись + тип `year | quarter | month`, до 48
4. Баланс — 7 агрегатов (`A_FIXED`, `A_INVENTORY`, `A_RECEIVABLE`, `A_CASH`,
   `P_EQUITY`, `P_LONG`, `P_SHORT`) + memo `M_RETAINED`, `M_MARKET_CAP`
5. ОФР — 6 статей (`I_REVENUE`, `I_COGS`, `I_OPEX`, `I_INTEREST`, `I_OTHER`, `I_TAX`)
6. **Методики** — `user_metrics`: свои показатели-формулы, до 100
7. **Свои нормативы** — `thresholds`: переопределяют универсальные пороги, до 100
8. **Переоценка статей** — `revaluations`: поправки с корреспонденцией в капитале, до 50

### Анализ (`audit_core/analysis.py`)
9. Аналитическая форма баланса с подытогами (Оборотные активы, СУММАРНЫЙ АКТИВ/ПАССИВ,
   Чистый оборотный капитал)
10. Аналитическая форма ОФР (Валовая прибыль, EBIT, Прибыль до налогообложения, Чистая)
11. **Горизонтальный анализ** — изменение и темп к предыдущему периоду; нулевая база → `None`
12. **Вертикальный анализ** — доля в активе / в выручке
13. **Коэффициенты — 4 группы, 16 показателей:**
    - ликвидность: текущая, срочная, абсолютная, ЧОК
    - устойчивость: автономия, обязательства/активы, обязательства/капитал, покрытие процентов
    - оборачиваемость: активов, запасов (дн.), дебиторки (дн.)
    - рентабельность: валовой, операционной, чистой прибыли, ROA, ROE
14. Приведение периодов к году (квартал ×4, месяц ×12) и дни периода
15. Инвариант актив = пассив, `balance_gap` по периодам
16. Оговорки результата (переоценки идут первыми)

### Диагностика (`audit_core/diagnostics.py`)
17. Альтман **Z′** (непубличные) и **Z″** (развивающиеся рынки)
18. **Классическая модель Альтмана** — только при введённой рыночной капитализации
19. Оценка коэффициентов против нормативов (универсальных или своих)
20. Сводный **светофор** `ok | warning | risk` + текст сводки

### Заключение (`audit_core/opinion.py`, `app/audit_docgen.py`)
21. Детерминированный текст заключения (без ИИ)
22. Выгрузка **DOCX** — `GET /subjects/{id}/report.docx`

### Группа (`audit_core/consolidate.py`, таблица `audit_groups`)
23. Свод по совпадающим подписям периодов
24. **Исключения** (`Elimination`): дебиторка/кредиторка, выручка/себестоимость,
    вложения в капитал, нереализованная прибыль в запасах
25. Предупреждение о невыключенных оборотах — выводится всегда
26. Оговорка при смешении основ отчётности; гудвилл и НКД не выделяются — оговорка
27. Сохранённые группы: хранится состав, а не результат; удалённый участник назван

### Фронтенд
28. `AuditHomePage` — список субъектов
29. `AuditSubjectPage` — 8 вкладок: Субъект · Ввод отчётности · Отчёты · Коэффициенты ·
    Тренды · Диагностика · Методики · Заключение
30. `AuditGroupPage` — свод группы
31. **Импорт отчётности из XLSX** (`auditXlsx.ts`) — round-trip шаблон, матч по коду/названию
32. **Выгрузка таблиц в XLSX** (`auditExport.ts`) — лист на раздел; неопределённый
    показатель = пустая ячейка, не ноль
33. Проверка инварианта на вводе (`auditBalance.ts`)
34. Переключатель продуктов и тема (`data-product`)

### Платформа (наследуется, менять не нужно)
35. RBAC: 5 ролей, 9 прав · организации (many-to-many) · тарифы · оплата ЮKassa

## 3. Обратная карта: у каждой возможности есть экран-хозяин

Главная страховка. Первым проходом **десять позиций оказались без хозяина** — макеты
рисуют due diligence и просто не содержат разделов финансового анализа. Ниже каждой
назначен дом; пункты со звёздочкой — расширение макетов, а не то, что на них нарисовано.

| Возможность | Экран-хозяин |
|---|---|
| 1 Реквизиты | Экран 6 — карточка дела, модалка создания |
| 2 Основа отчётности \* | Экран 2 — шапка ввода; уже в шапке DOCX |
| 3 Периоды (год/квартал/месяц) \* | Экран 2 — колонки таблицы |
| 4–5 Ввод баланса и ОФР | Экран 2 — режим ввода |
| 6 Методики \* | Экран 21 — «Чек-лист процедур» расширяется до «Методика проверки» |
| 7 Свои нормативы \* | Экран 21, там же |
| 8 Переоценка статей \* | Экран 7 — та же машинерия, что нормализация EBITDA |
| 9–10 Аналитическая форма | Экран 2 — колонка «по отчёту» |
| 11–13 Тренды и коэффициенты \* | **Новый раздел «Финансовое состояние»** дела |
| 14–16 Приведение, инвариант, оговорки | Экран 2 + Экран 19 «Ошибки данных» |
| 17–20 Альтман и светофор \* | Экран 1 — вердикт получает вторую опору, кроме DCF |
| 21–22 Заключение и DOCX | Экран 5 + печатный бланк |
| 23–27 Группа и исключения \* | Экран 14 — панель исключений внутри экрана группы |
| 28 Список | Экран 6 |
| 29 Вкладки дела | Экраны 1, 2, 4, 7, 9, 10, 13, 17 + «Финансовое состояние» |
| 30 Свод группы | Экран 14 |
| 31 Импорт XLSX \* | Экран 19 — один из источников загрузки |
| 32 Выгрузка XLSX | Экран 8 (фоновая выгрузка), Экран 14 |
| 33 Проверка инварианта | Экран 19 |
| 34 Переключатель продуктов | Экран 6 — шапка каркаса |
| 35 RBAC, тарифы, оплата | Экраны 11, 15, 16 |

**Два расширения макетов приняты осознанно:**

- **Раздел «Финансовое состояние»** (тренды + коэффициенты + диагностика). На макетах
  его нет: они ведут покупателя от скана к цене и не показывают анализ финсостояния
  цели. Выбрасывать 16 показателей, горизонталь, вертикаль и три модели Альтмана
  ради полноты картинки нельзя — это работающая, оттестированная часть продукта.
- **Вердикт опирается на две опоры.** У макета вердикт «покупать / торговаться /
  отказаться» выводится из DCF-моста. Светофор и Альтман отвечают на другой вопрос —
  «выживет ли цель», — и он для покупателя не менее важен. Обе опоры показываются
  раздельно и **не смешиваются в одно число**.

## 4. Чего в макетах нет вовсе — методики

Экраны 1, 4, 7, 9, 10, 21 — ядро due diligence, и **числа в них выдуманы для показа**
(дело A-2417, EV 1767, семь флагов на 220 млн ₽). Макеты не содержат: правил 24 процедур,
способа назначить флагу цену в рублях, состава корректировок EBITDA, модели DCF.

Порядок для этого проекта обратный тому, что предлагает хендофф: **сначала методика в
`docs/FINANS-AUDIT-SPEC.md` и свой golden, потом экран.** Красивый интерфейс над
числами, которым нельзя верить, — единственное, чего продукт себе позволить не может.

Машинерия при этом уже есть и трижды обкатана:

| Что строим | На чём |
|---|---|
| Реестр флагов | `calc_core/review/` — `Finding` с числовым evidence, категории, светофор |
| Нормализация EBITDA | `revaluation.py` — поправка с корреспонденцией, никогда не молчаливая |
| DCF и мост EV → цена | `build_investment_metrics`, дисконтирование, `_fx_series` |
| Чувствительность, Монте-Карло | `calc_core/sensitivity.py`, асинхронный MC |
| Чек-лист процедур | `user_metrics` + `thresholds` — формулы и пороги уже есть |

## 5. Фазы

### Фаза 0 — инвентарь и решения ✅ (этот документ)

### Фаза 1 — токены и каркас
- [ ] Палитра хендоффа в `styles.css`: обе темы + тепловая карта + палитра печати
- [ ] `CubeHero` получает продуктовую палитру (проп `accent` уже есть)
- [ ] Каркас по Экрану 6: боковая навигация, брейкпоинты 1180 / 864 / 402
- [ ] Три текущие страницы переезжают **целиком**, все 8 вкладок на месте
- [ ] Доступность таблиц по §7d хендоффа: `role="table"`, `aria-rowcount` полный,
      фокус строки, статус словом а не только цветом

### Фаза 2 — дело как контейнер
- [x] Экран 6 — список дел, карточки, фильтры, модалка создания, пустое состояние
- [x] Дубль дела (`POST /subjects/{id}/duplicate`) и удаление с подтверждением,
      называющим объект и последствие
- [x] Светофор диагностики в карточке (`AuditSubjectSummary.light`). Считается на лету
      из той же диагностики, что и вкладка дела: хранить его отдельно значило бы
      показывать результат, разошедшийся с отчётностью после первой же правки.
      **`null` ≠ `ok`** — «отчётности нет» и «в норме» разные факты.
- [x] Экран 19 — качество данных: `audit_core/input_check.py`, 8 детерминированных
      правил над моделью (несходящийся баланс, отрицательные статьи, себестоимость без
      выручки, прибыль больше капитала, половина отчётности, пустой период, подписи
      периодов). Каждая находка несёт тяжесть, периоды и числа. **В `AuditResult` не
      входит** — качество ввода не результат анализа, а высказывание о его входе,
      поэтому golden аудита не задет.
- [x] Экран 22 — поиск по делам (название и отрасль, без учёта регистра и «ё»)
- [x] Экран 18 — демо-дело: `POST /subjects/demo` заводит **обычное дело** из
      эталонного семпла. Не режим просмотра: его правят, дублируют и удаляют теми же
      кнопками, иначе «только чтение» пришлось бы тянуть через весь редактор ради
      экрана, который видят один раз. Пометка о вымышленности — **в имени**: имя едет
      с делом в список, в свод группы и в шапку DOCX-заключения, а флаг в базе читали
      бы только там, где не забыли, и однажды демонстрация ушла бы наружу как настоящая
      проверка.

**Ленты событий (Экран 22) не будет, пока нет журнала.** Макет показывает поток
событий по всем делам, но таблицы `audit_log` в базе нет (пробел №13 сверки, открыт).
Пустая лента обещала бы историю, которую никто не ведёт; появится вместе с журналом.

**Состояния «результат устарел» у аудита нет и не будет.** В «Элит» оно означает
`updated_at > last_calculated_at` — расчёт сохранён и мог отстать от модели. Аудит
считает анализ на каждый запрос и ничего не хранит, поэтому устареть нечему: показать
плашку «устарело» значило бы выдумать несуществующее состояние. Пробел №6 из
`ПРОБЕЛЫ-сверка-с-репозиторием.md` закрывается этим решением, а не экраном.

### Фаза 3 — существующее по макетам
- [x] Разделы дела по макетам: Субъект · Отчётность (Экран 2) · **Финансовое
      состояние** · Методики · Заключение. Вкладки не свёрнуты и не переписаны —
      **сгруппированы**: внутри раздела с несколькими вкладками появляется вторая
      полоса, содержимое каждой прежнее. Инвариант «ни одна вкладка не потеряна»
      закреплён тестом и проверен мутацией.
- [x] Экран 5 + печатный бланк A4 (794×1123, поля 15/16/11 мм): два листа, палитра
      печати задана на самом листе (бумага не зависит от темы), отрицательные значения
      в скобках, `@page` живёт только пока бланк открыт — иначе перевернуло бы
      альбомный отчёт «Элит».
- [ ] Экран 14 — группа с панелью исключений
- [x] Экран 15 — доступ и профиль: **активация приглашения** и профиль (имя, смена
      пароля). До этого приглашение вело в никуда — участник заводился без пароля
      (`crud.add_member`), а способа его задать не существовало вовсе, при этом
      интерфейс сообщал «Приглашение отправлено», хотя писем платформа не шлёт.
      Токен приглашения **не является токеном входа** (`decode_token` проверяет
      назначение) и срабатывает один раз: иначе ссылка осталась бы вечным способом
      сбросить чужой пароль. Ссылку передаёт пригласивший — она показывается ему
      после приглашения и **не выдаётся в списке участников**.
- [x] Экраны 11, 16 — организация (участники · журнал · профиль · тариф) и оплата

**Восстановления пароля не будет, пока нет доставки писем.** Честный сброс требует
письма на подтверждённый адрес; эндпоинт «сброшу по e-mail» без письма — это способ
угнать любой аккаунт, зная только адрес. Отсутствие закреплено тестом: в контракте
нет ни одного пути с `reset` / `forgot` / `recover`.

**Экран 14 — панель исключений уже была** (`Elimination` v2). Структуры владения и
связанных сторон на ней нет и не планируется тем же решением, что гудвилл и
неконтролирующая доля: статей под них в аналитической форме не существует.
- [x] Таблица `audit_log` (обратный пробел №13): миграция `d4f6a8c0b2e3` с RLS,
      запись действий над делами и составом организации, `GET …/audit-log` под правом
      `org.manage`, вкладка «Журнал доступа». **Только чтение** — ни PUT, ни DELETE;
      срок хранения (5 лет) — политика эксплуатации, а не логика приложения: чистка
      журнала кодом означала бы, что приложение умеет стирать собственные следы.
      Почта актора дублируется в записи: журнал обязан отвечать «кто это сделал» и
      после удаления участника.
- [x] Прайс: **свой каталог на каждый продукт**. Подписка стала своей у «Элит» и
      «Аудита» (`subscriptions` уникальна по паре организация + продукт, миграция
      `e5b7c9d1f204`), коды тарифов уникальны глобально — продукт выводится из кода,
      а не приходит вторым полем, иначе два источника правды разошлись бы. Цены «Элит»
      не тронуты; у Аудита — прайс макета (Пробный 0 ₽ / Команда 24 000 ₽ / Корпоративный
      по запросу). «По запросу» — **отдельный признак, а не цена 0**: ноль на экране
      читается как «бесплатно».
- [x] **Дела теперь считаются квотой.** Раньше не считались вовсе:
      `ensure_project_quota` смотрел только `count_projects`, а создание, дубль и демо
      дела квоту не вызывали — на любом тарифе дел можно было завести сколько угодно.
      Единица квоты у продуктов разная, поэтому поле каталога называется `max_units`,
      а не `max_projects`: имя, верное лишь для половины каталога, однажды прочитают
      буквально.

**Разовой проверки «45 000 ₽ за дело» из макета в каталоге нет.** Это оплата за
использование, а не подписка: счёт выставляется на каждое заведённое дело, а не раз в
месяц, и тарифом с квотой в одно дело это не выражается. Платёжный поток
(`payments_yookassa`) начинается со смены тарифа, а не с создания сущности — завести
такой «тариф» значило бы изобразить биллинг, которого нет.

### Фаза 4 — ядро due diligence (методика → golden → экран) — **закрыта**
- [x] **Реестр флагов (Экран 9)** — методика в SPEC, Приложение И; `audit_core/flags.py`,
      8 правил над агрегатной отчётностью. **В `AuditResult` не входит** — как ревью
      бизнес-плана не входит в `CalcResult`: находка над результатом не меняет методику
      расчёта, и новые правила не шевелят эталон чисел.
      **Денежная мера есть не у всякого флага** (`impact: Decimal | None`): отрицательный
      капитал и непокрытые проценты не выражаются суммой, которую покупатель вычтет из
      цены. Итог называет сумму оценённых **и число неоценённых** — иначе он читался бы
      как полная цена рисков. Когда мера не определена ни у одного, итог говорит «не
      определено», а не «0 ₽».
- [x] **Качество прибыли (Экран 7)** — методика в SPEC, Приложение К;
      `audit_core/earnings.py`. **EBITDA из формы не выводится**: амортизации в
      аналитической форме нет, поэтому введена справочная строка ОФР `M_DEPRECIATION`.
      Введена — считаем EBITDA, не введена — нормализуем **EBIT и называем его EBIT**:
      подписать одно другим значило бы сдвинуть мультипликатор сделки на всю
      амортизацию. Корректировки задаёт пользователь (нормализация — суждение, а не
      расчёт) и **каждая обязана иметь причину**: безымянная не применяется.
      Буква A/B/C — объявленное соглашение о шкале, а не измерение, и рядом всегда
      стоит расхождение в процентах.
- [x] **Обязательства и залоги (Экран 10)** — методика в SPEC, Приложение Л;
      `audit_core/obligations.py` + 4 правила в реестре флагов. Реестр — **вводимые
      данные**: из двух строк-агрегатов не видно ни кому должны, ни под какой залог,
      ни что случится при нарушении ковенанта.
      **Забалансовое не складывается с балансовым**: итога два, общей суммы у них нет
      и на экране сказано почему — сложить значило бы утверждать, что поручительство
      уже наступило (тот же приём, что с рыночной капитализацией при своде группы).
      **Сверка с балансом показывается всегда**, включая «сошлось»; флагом становится
      только существенное расхождение и только у заполненного реестра — пустой реестр
      это «не заполнено», а не «обязательств нет».
      **Ковенант — состояние, а не вычисление**: условие договора («Долг/EBITDA ≤ 3.0»)
      к нашим 16 показателям не сводится, поэтому статус ставит человек, а `unknown` —
      значение по умолчанию и **не выдаётся за благополучие**.
      **График — долг по годам погашения, а не платежи года**: амортизация долга в
      модель не вводится, «равные доли по годам» были бы выдуманными условиями
      договора. «По требованию» и «срок не заполнен» — разные корзины.
      Демо-дело несёт сошедшийся реестр, задевающий каждое правило и **не поднимающий
      ни одного флага** — иначе экран учил бы мириться с расхождением.
      Попутно: контракт зеркала (`api/model.contract.test.ts`) расширен на `api/audit.ts` —
      12 полей `Obligation` написаны руками, и переименование любого из них молча
      теряло бы введённое значение.
- [x] **Чек-лист процедур (Экран 21)** — методика в SPEC, Приложение М;
      `audit_core/procedures.py`, каталог из 28 процедур (счёт закреплён тестом).
      **Исполнитель назван у каждой**: `system` — процедуру выполняет правило
      платформы, `analyst` — ей нужны первичные документы (выписки, картотека судов,
      справка ФНС, реестр ФНП), которых в модели нет. Макет подписывает «базовая ·
      автоматически» под сверкой с банковской выпиской; так подписать нельзя —
      платформа не выполняет того, чего не читает, и это **другой исполнитель**,
      а не «пока не реализовано» (проверено тестом на запрещённые формулировки).
      **Статус системной процедуры выводится из прогона**, а не ставится руками:
      отметка аналитика по ней игнорируется. `no_data` — **не «пройдено»**: покрытие
      процентов без введённых процентов не проверено, а не благополучно; модель
      Альтмана без нераспределённой прибыли не посчитана, а не «в зелёной зоне».
      **Снятие без причины не применяется** — процедура, снятая молча, неотличима
      от забытой.
      **Границы проверки** собирают всё невыполненное и попадают в заключение
      **и в DOCX**: читатель документа и есть тот, кому они адресованы, а умолчание
      о непроверенном он прочтёт как проверенное. Охват без списка границ не
      выводится.
      **Отраслевого каталога нет намеренно** (М.5): набор «что проверяют в
      грузоперевозках» — методика, которой у платформы не существует. Вместо
      выдуманного набора — свои процедуры аналитика, рядом с базовыми в заключении.
- [x] **Риск-скан и вердикт (Экран 1)** — методика в SPEC, Приложение Н;
      `audit_core/summary.py`. Сводка **ничего не считает заново**: собирает светофор,
      флаги, качество прибыли, реестр обязательств и чек-лист. Единственная новая
      величина — **Долг / нормализованный EBITDA** (к устойчивому заработку, а не к
      отчётному); при неположительном показателе **не считается**: долг к убытку не
      измеряется кратностью — вышло бы отрицательное «плечо», читаемое как малый долг.
      **Вердикт неотделим от охвата** и стоит с ним в одной карточке: «критических
      отклонений не выявлено» при охвате 61% — это оценка шести десятых работы.
      **Ошибка ввода отменяет вердикт целиком** (`unreliable`), а не понижает его:
      числа из несходящегося баланса нельзя ни подтвердить, ни опровергнуть.
      **Тяжёлый флаг не даёт зелёного**, каким бы ни был светофор (тот же приём, что
      `NPV < 0` в заключении первого продукта).
      **Оценки сделки нет — и это главное решение экрана.** Макет показывает «Дисконт
      к цене 18%» и «Справедливо 1 020 млн ₽ вместо запрошенных 1 240 млн ₽»: ни одной
      из трёх величин у платформы не существует — запрошенной цены в модели нет, DCF не
      построен, бенчмарков нет. Число, выведенное из ничего, унесли бы в переговоры.
      На его месте — оценённое влияние флагов, и прямо сказано, что скидкой оно **не
      является**. Так же не показываются подтверждение выручки выпиской и сравнение с
      отраслью; **всё, чего сводка не считает, перечислено на ней списком** — иначе
      отсутствие раздела читается как благополучие.
      **Прогресса скана нет** (Н.4): анализ занимает миллисекунды, и полоса «04:12 из
      ≈06:00» обещала бы глубину проверки количеством потраченного времени. Состояния
      два: отчётность не введена (с переходом к вводу) и сводка.

### Фаза 5 — оценка
- [x] **DCF и мост EV → цена (Экран 4)** — методика в SPEC, Приложение П;
      `audit_core/valuation.py`. **Прогноз вводится**: в деле есть только прошлое, и
      экстраполировать выручку «как росла, так и будет» значило бы выдать регрессию за
      прогноз. Единственная связь с проверкой — **база прогноза нормализованная**
      (Прил. К), ради чего нормализация и делалась; база FCFF — **всегда EBIT**, даже
      когда мультипликатор к EBITDA: амортизация стоит в формуле отдельным слагаемым.
      **Без амортизации оценка не считается** — ноль занизил бы поток ровно на неё;
      **убыточный бизнес не оценивается**; **терминальной стоимости не существует при
      `g ≥ WACC`**. Каждый отказ назван причиной, а не показан нулём.
      **Долг в мосте — из реестра обязательств** (Прил. Л): это именно процентный долг.
      Реестра нет — агрегат `P_LONG + P_SHORT` **с оговоркой**, что он включает
      кредиторку и занижает цену. **Забалансовое из EV не вычитается** (Л.1), но
      названо: условное обязательство ещё не наступило.
      **Дисконт требует обоих операндов** — посчитанной оценки и введённой цены
      продавца; нет любого — дисконта не существует, а не «ноль процентов». Оценка выше
      запрошенной подаётся премией со знаком.
      **Диапазон — это чувствительность** (сетка 5 × 5 WACC × g), а не три сценария:
      они отвечали бы на тот же вопрос втрое дороже. Невозможная клетка пуста, а не
      равна нулю.
      **Сопоставимых сделок и IRR сделки нет** — базы аналогов и допущения о выходе не
      существует; названы списком, как на Экране 1.
      Экран 1 замкнут: обещанный в Прил. Н.3 дисконт появляется ровно тогда, когда
      оценка посчитана и цена продавца введена, и **закрывает ровно один** пробел из
      своего списка.
- [x] **Анализ рисков (Экран 13)** — методика в SPEC, Приложение Р; `audit_core/risk.py`.
      Своей арифметики у модуля нет: каждый прогон — тот же `build_valuation` при
      смещённых допущениях, поэтому разойтись с методикой оценки он не может.
      **Смещение мультипликативное и одно для всех параметров** (`1 ± шаг`): смесь
      «±1 п.п. для ставок, ±10% для сумм» делала бы столбцы несопоставимыми, а порядок
      торнадо — следствием выбранных шагов. Шаг **подписан у каждого столбца** и
      настраивается: торнадо, скрывающий свой шаг, выдаёт соглашение за измерение.
      Сторона, которой не существует, не заменяется базой; незаданное допущение названо
      незаданным. Число красится **по знаку**, а не по стороне смещения — у ставки
      дисконтирования снижение поднимает цену, и «+162» в красном читалось бы потерей.
      **Монте-Карло**: распределения **коэффициентов** задаёт пользователь (то же
      соглашение, что в первом продукте). Прогон воспроизводим по фиксированному `seed` —
      иначе медиану нельзя назвать за столом переговоров. **Прогон без оценки не
      заменяется нулём и не выбрасывается молча**: считается отдельно, доля называется.
      Неполное распределение исключается с оговоркой, а не подменяется умолчанием.
      Медиана сверяется с базой (расхождение > 10% — сообщается). Вероятность «ниже
      запрошенной» — только при введённой цене продавца.
      На экране прямо сказано: числа **ровно настолько хороши, насколько верны заданные
      распределения**, — это условие чтения блока, а не сноска.
      **Сценариев с вероятностями нет** (взвешенная «ожидаемая цена» — сумма догадок,
      выданная за расчёт) и **стресс-теста «потеря контрагента» нет** (разреза выручки
      по контрагентам в модели не существует); оба пробела названы.
- [x] **Сравнение дел (Экран 20)** — методика в SPEC, Приложение С;
      `audit_core/compare.py`, `POST /api/v1/audit/compare`, страница `/audit/compare`.
      Модуль **ничего не считает**: каждое число уже посчитано в своём деле. Сравнение
      разовое, как свод группы: хранится запрос, а не результат.
      **«Кто лучше» — только там, где «лучше» определено.** У Enterprise Value и цены за
      долю направления нет: больше — это размер, а не качество сделки. У оценённого
      влияния флагов тоже нет: абсолютная сумма несопоставима между бизнесами разного
      размера — сравнимо **число** тяжёлых флагов. Победитель выводится, лишь когда
      значение есть у всех; ничья победителя не даёт.
      **Сводного балла с весами нет**: балл прячет веса за собой, и два дела с разными
      весами получают разные баллы при тех же числах. Вместо него — счёт побед по
      **видимым построчно** показателям.
      **Рекомендации по сделке нет**: выбор зависит от стратегии покупателя, портфеля и
      финансирования — ничего этого в деле нет, и платформа, рекомендующая сделку,
      притворялась бы инвестором.
      **Оговорки сопоставимости выводятся**: валюты, отрасли, основы отчётности,
      последние периоды, показатель нормализации, охват. Различие валют — единственная,
      которая **снимает победителей** у денежных строк.
      Дело без отчётности выбрать нельзя — с объяснением, а не молча; попавшее в запрос
      называется в оговорке.

### Фаза 5 закрыта

### Фаза 6 — остальное
- [x] **План-факт после сделки (Экран 17)** — методика в SPEC, Приложение Т;
      `audit_core/planfact.py`. **План вводится** из меморандума — вывести его платформа
      не может; **факт вводить не нужно**: это отчётность дела, и второго источника
      фактических чисел план-факт не заводит.
      Период без плана в сравнение не идёт, охват перечислен. **Нулевой факт показан как
      полный недобор и одновременно назван двусмысленным**: ноль значит либо «выручки не
      было», либо «период ещё не отражён», и молча выбрать толкование нельзя.
      Строка сворачивает периоды **по типу величины**: потоки складываются, остатки
      берутся на конец — сложить остаток за два года значило бы получить величину,
      которой не существует ни в один момент.
      **Направление объявлено у каждой строки**: себестоимость ниже плана — успех, а не
      недобор; порог существенности 10% — соглашение.
      **«Флаг сработал» — суждение аналитика**: платформа видит отчётность, а не причины.
      Предсказанное влияние посчитано ею, фактическая потеря введена человеком — обе
      половины подписаны, чтобы «дисконт окупился» не читалось как один расчёт. Флаг без
      денежной меры не сопоставляется; отметка по исчезнувшему флагу названа, а не
      выброшена; невведённая потеря — не ноль.
      **Доходности вложения нет**: нужна цена, по которой сделка закрылась, а запрошенная
      её не заменяет — торг на то и был.
- [x] **Аутентификация (Экран 3) и сплеш** — каркас входа/регистрации остался один на
      оба продукта, содержимым панели и марки он теперь **следует продукту**: у `/login`
      своего продукта нет, поэтому берётся последний выбранный (`currentProduct()` —
      адрес, иначе `localStorage`). Вход, регистрация и сплеш перестали быть зелёными
      экранами «Элита» на входе в «Аудит», и успешный вход ведёт в тот продукт, из
      которого пришли, а не всегда в список проектов.
      Тема применяется до отрисовки карточки (`useAuthProduct`), иначе форма оставалась
      бы зелёной посреди фиолетовой панели. Зелёный в правилах `.auth-brand` был записан
      цветом, а не токеном (панель рисовалась под один продукт) — для аудита те же места
      переведены на токены темы, второй копии панели не заведено. Сплеш — единственный
      экран без темы на `<html>` (рисуется раньше каркаса), поэтому у него свой класс
      `.splash--audit` с явными цветами, как и у зелёного варианта.
      **Обещаний макета в панели нет**: «24 процедуры проверки», «06:00 среднее время
      скана» и «18% средний дисконт к цене» — фонового скана в продукте не существует
      (анализ считается сразу и на месте), а статистики по чужим сделкам платформа не
      собирает. Панель называет то, что действительно посчитается по введённой отчётности.
      **Кнопки SSO нет** — входа по учётной записи организации в продукте не существует,
      и мёртвая кнопка на экране входа хуже её отсутствия.
      **Отсутствие восстановления пароля названо прямо**: сбросить пароль не может ни
      пользователь, ни администратор (приглашение задаёт пароль однажды), поэтому ссылка
      «Забыли пароль?» вела бы в никуда, а молчание — к поиску того, чего нет.
      **Именного счётчика попыток нет**: «осталось 3 попытки до блокировки» обещало бы
      счёт по учётной записи, а сервер ограничивает попытки с адреса за минуту — 429
      теперь и объясняется этими словами.
- [x] **Онбординг (Экран 12)** — `/audit/onboarding`: рейл со шагами, три шага ввода и
      результат. Мастер **заводит настоящее дело**, а не показывает экскурсию: собирает
      то, что действительно есть в модели (реквизиты, периоды) и в организации
      (участники), и создаёт дело **одним запросом на последнем шаге** — брошенный на
      полпути мастер не оставляет полупустых дел. Вход — с пустого списка дел
      («Настроить пространство»); быстрая модалка осталась для второго и последующих.
      Подписи годов предлагаются по **последним завершённым**: отчётность за идущий год
      ещё не сдана. У кварталов и месяцев подписи не угадываются, и смена периодичности
      их стирает — угаданные годы у кварталов были бы мусором.
      **ИНН и подтягивания форм 1 и 2 из ФНС нет**: такой интеграции у платформы не
      существует, а поле, которое ничего не подтягивает, обещало бы её наличие (и
      хранить его негде — реквизита в модели субъекта нет). Отчётность вводится на
      вкладке «Ввод» или импортируется из XLSX — так и сказано на шаге.
      **Глубины проверки «экспресс · 8 / полная · 24 / для банка · 31» нет**: чек-лист
      процедур один на все дела, сокращённый набор не убрал бы работу, а спрятал бы
      процедуры, которые всё равно делать; «31» в каталоге просто нет.
      **Роли организации, задающей набор процедур и шаблон заключения, нет**: ни то, ни
      другое от роли в продукте не зависит — сохранённая роль ни на что не влияла бы.
      **Доступ — организационный**: участник видит все дела, журнал общий; «доступ
      выдаётся к делу, а не ко всему пространству» из макета описывает другую модель
      прав, и на шаге сказано, какая работает здесь. Приглашение **отдаёт ссылку
      активации** (как и на странице участников): писем платформа не отправляет.
      **Фонового скана и письма о готовности нет**: анализ считается сразу и на месте —
      это первое, что говорит экран результата, вместо прогресса несуществующей проверки.
      Попутно: `SelectField` наконец связывает подпись с селектом (`htmlFor`/`id`, как у
      `Field`) — до этого каждый селект обоих продуктов скринридер читал безымянным.

### Фаза 6 закрыта — экраны хендоффа разобраны все

### Фаза 7 — заключение вбирает ядро проверки

- [x] **Документ и печатный бланк (Экран 5)** — методика в SPEC, Приложение У.
      Обнаружено при приёмке: конвейер слоёв был записан **дважды** — в эндпоинте
      анализа и в выгрузке документа, — и во второй копии половина слоёв
      отсутствовала. Документ рассказывал о финансовом состоянии и молчал о находках,
      качестве прибыли, обязательствах, оценке и рисках — при том, что читатель
      документа и есть тот, кому они адресованы. Копия теперь одна:
      `audit_core/pipeline.py::review_case`, оба выхода зовут её; инвариант «экран и
      документ идут из одного конвейера» закреплён тестом.
      В DOCX и на бланк добавлены вердикт с охватом проверки, реестр флагов, качество
      прибыли, обязательства, оценка и риски (в DOCX ещё и план-факт). Порядок —
      порядок чтения инвесткомитетом: сначала вывод и находки, потом числа под ними.
      **Оговорки перенесены на бумагу вместе с числами**: списки «не посчитано» каждого
      раздела, «оценённое влияние флагов — не скидка к цене» и число флагов без
      денежной меры рядом с суммой, «меры нет» вместо нуля рублей, забалансовые
      обязательства отдельной величиной, условие чтения Монте-Карло, подписи источников
      в план-факте, границы проверки на бланке.
      Число листов бланка **считается**, а не пишется: разделы условны, и «1 / 2» на
      трёхстраничном документе — ошибка в самом документе.
      **Подписей исполнителя и партнёра нет** — в деле нет ни того, ни другого, а пустые
      линии под чужими должностями предлагали бы документу вид, которого он не имеет.
      **Пресетов бланка «полное / только вердикт / для банка» нет** (та же причина, что
      у глубины проверки). **Сверки «отчёт против проверки» по строкам отчётности нет** —
      второго источника чисел у платформы не существует; настоящая сверка в продукте
      одна, и она напечатана: долг по реестру против долга по балансу.
- [x] **Выгрузка XLSX** — тот же состав, что в документе (SPEC, Приложение У.5): листы
      вердикта, флагов, качества прибыли, обязательств, оценки, рисков и план-факта
      перед листами финансового состояния. Таблицу просят ради переноса чисел в чужую
      модель — тем важнее, что **флаг без денежной меры выгружается пустой ячейкой и
      словами**, а не нулём, и что оговорка «оценённое влияние флагов — не скидка к
      цене» напечатана в самом файле: колонку с суммой легко перенести как скидку.
      Правило пустоты — одно на книгу: дело без единого периода не выгружается вовсе,
      а непосчитанный раздел внутри непустого дела лист получает — с препятствиями.
- [x] **Приёмка выгрузки и сравнения** — две находки того же рода, что фаза 7a.
      **Свод группы получал листы проверки, которой не было**: слои считаются только у
      дела, свод идёт мимо конвейера, и его ответ несёт умолчания — в файл группы
      попадали лист обязательств с нулями и лист «оценка не посчитана». Признак
      посчитанности — `summary.state`; листы проверки строятся только при `ready`.
      **Сравнение дел держало третью копию конвейера** — свой порядок слоёв рядом с
      общим. Переведено на `review_case(deep=False)`: стохастический слой пропущен
      (в колонках его нет, а Монте-Карло на четыре дела — тысячи перестроений оценки),
      всё остальное считается один раз и одинаково. Пропущенный слой остаётся при
      умолчаниях **с названной причиной**, а не при нулях. Оба инварианта закреплены
      тестами: «режим не меняет ничего, кроме рисков» и «колонка сравнения равна числу
      самого дела».

Лендинг (`docs/design/finans-audit/landing/`) — отдельный статический сайт вне
`frontend/src`, в эти фазы не входит.

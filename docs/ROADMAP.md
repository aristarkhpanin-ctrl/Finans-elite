# Дорожная карта развития

> **Назначение.** Пошаговый план развития продукта (SaaS-замена Project Expert 7.21).
> Определяет порядок работ, критерии готовности каждого шага и влияние на версию
> расчётного ядра. Главный приоритет — **точность расчёта** (финансовый продукт).
>
> **Опирается на:** [`RESEARCH-ProjectExpert.md`](./RESEARCH-ProjectExpert.md),
> [`ARCHITECTURE-SaaS.md`](./ARCHITECTURE-SaaS.md),
> [`CALC-ENGINE-SPEC.md`](./CALC-ENGINE-SPEC.md).
>
> **Дата:** 2026-07-03 · **Текущая фаза:** B (расширение ядра) · `engine_version = 0.9.9`
>
> **UI-редизайн «Modal — зелёный куб» — завершён** (отдельный трек, план и статус —
> [`REDESIGN-DECOMPOSITION.md`](./REDESIGN-DECOMPOSITION.md)): все экраны переведены на
> новую дизайн-систему; backend-доработки под богатый UI — B1 (сводка последнего расчёта),
> B2 (дубль проекта), B3 (участники холдинга + разбивка консолидации), B4 (управление
> участниками орг), B5 (гистограмма Монте-Карло). Golden без дрейфа. **Тестов backend: 293.**

---

## Где мы сейчас (готово)

**Расчётное ядро `calc_core`** (детерминированная функция `run(model) → CalcResult`):
четыре отчёта (ОПУ, Кэш-фло, Баланс, Использование прибыли), показатели эффективности
(NPV/IRR/PI/PB/DPB), финансовые коэффициенты (5 групп), безубыточность, оборотный
капитал (дебиторка/кредиторка/авансы), запасы (сырьё/ГП), НДС с зачётом, амортизация,
налоги (прибыль/имущество), займы, акционерный капитал, автоподбор финансирования,
анализ (чувствительность, Монте-Карло, What-If), оценка бизнеса (чистые активы + модель
Гордона), актуализация (план-факт), консолидация (Integrator). Все суммы — `Decimal`.
Балансовый инвариант проверяется в рантайме.

**Backend (FastAPI):** аутентификация (JWT/Argon2), мультиарендность (организации,
RBAC), проекты (CRUD), расчёт, биллинг (тарифы, квоты, ЮKassa + ручной провайдер,
54-ФЗ чек, идемпотентный webhook), холдинги. Alembic-миграции, Docker, CI.

**Frontend (React/TS):** аутентификация, проекты (создание из шаблонов по типам бизнеса),
редактор модели (все настройки ядра: налоги/НДС/инфляция/цикл/оценка, подсказки и
валидация), результаты (сводка-вердикт, метрики + оценка бизнеса, таблицы + графики,
экспорт CSV/XLSX/PDF), анализ (чувствительность/Монте-Карло/What-If), план-факт, холдинги,
организация и биллинг.

**Тесты:** 275 (юнит, инвариант-property, golden-master, аналитические, API/биллинг).

**Статус версии.** Методика ещё не сверена по всем пунктам
[`CALC-ENGINE-SPEC.md §22`](./CALC-ENGINE-SPEC.md#22-точки-требующие-валидации-по-эталону),
поэтому `engine_version` держится как предварительная (`0.x`).

---

## Принципы развития

1. **Точность прежде всего.** Любое изменение методики проходит через golden-master и
   аналитические тесты; диффы чисел просматриваются осознанно.
2. **Поэтапно, малыми шагами.** Один связный шаг за итерацию; балансовый инвариант
   `B20 = B34` сохраняется на каждом шаге.
3. **Версионирование методики.** Изменение правил расчёта ⇒ новая `engine_version`;
   старые golden-снимки замораживаются под старую версию.
4. **Чистое ядро.** `calc_core` не зависит от БД/HTTP/арендатора — его легко тестировать
   и переиспользовать (в т.ч. для фоновых воркеров).
5. **Без проприетарного кода.** Реализация — стандартная финансовая методология;
   совпадение цифр достигается сверкой по эталону, а не копированием.

---

## Фаза A — Точность ядра  ← **текущая**

**Цель фазы:** закрыть все пункты `§22`, расширить golden-набор и зафиксировать
`engine_version = 1.0` (методика сверена и стабильна).

| Шаг | Что делаем | Как проверяем | Версия | Статус |
|---|---|---|---|---|
| **A0** | Golden-master инфраструктура: канонический снимок результата (`serialize.py`), регрессионный тест с перегенерацией, аналитические ручные проверки | `test_golden`, `test_analytic` | — | ✅ готово |
| **A1** | **График инвестиций для NPV/PI** (§22.4): правило «прирост дефицита относительно максимума предыдущих периодов»; вынесен `Inv[t]`, PI = `1 + NPV/PV_инв`, добавлены `pv_investments` и `peak_financing_need` | аналитический тест на замкнутом потоке + golden-дифф | `0.7.0` | ✅ готово |
| **A2** | **Проводка I24** (§22.1): «издержки, отнесённые на прибыль» — невычитаемые (вне `I26`), уменьшают `I28`; источники `from_profit` / `interest_on_profit` | аналитический тест (издержка «из прибыли») + относительный (проценты «на прибыль») | `0.7.1` | ✅ готово |
| **A3** | **Момент признания НДС** (§22.2): режим «по оплате» (vs «по отгрузке») через `vat_basis`; отложенный исходящий НДС → `B21`, входной вне зачёта → `B7` | аналитический тест на сдвиг оплаты + property (баланс) | `0.7.2` | ✅ готово |
| **A7** | **Налог на прибыль** (§22.7): перенос убытков `I22` (последовательный пул) + льгота `profit_tax_benefit_share` | аналитический тест на убыточный → прибыльный период + property (баланс) | `0.7.3` | ✅ готово |
| **A6** | **Коэффициенты** (§22.6): оборачиваемость и ROA/ROE — на средние за период (начало t=0 = стартовый баланс); ликвидность/структура/«на акцию» — на конец | golden-дифф + аналитический тест | `0.7.4` | ✅ готово |
| **A5** | **Автоподбор** (§22.5): привлечение покрывает накопленные проценты; внешняя итерация с адаптивным демпфированием | правило «процент = долг × ставка», сходимость при 200% | `0.7.5` | ✅ готово |
| **A8** | **Запасы ГП** (§22.8): метод оценки себестоимости — средняя/ФИФО (`inventory_method`); НЗП (B4) длительного цикла → Фаза B | ФИФО vs средняя на разной себестоимости + property (баланс) | `0.7.6` | ✅ готово |
| **A4** | **Курсовая разница I25** (§22.3): валютный контур (`fx_rate`/`fx_open`), переоценка монетарной позиции `foreign_monetary` → `I25`/`B6` | аналитический тест на скачок курса + property (баланс) | `0.8.0` | ✅ механизм (Фаза B) |
| **A9** | **Расширение golden-набора:** отраслевые приёмочные сценарии (производство, торговля, услуги, проект «с нуля» и действующее предприятие) | golden-master на каждый сценарий | `0.9.9` | ✅ 4 сценария: `sample` (с нуля, производство), `showcase` (действующее, все подмодели), `trade` (опт), `services` (консалтинг) |
| **A10** | **Фиксация `1.0`:** все §22 закрыты, golden стабилен → `engine_version = 1.0` | весь набор зелёный | → `1.0.0` | ⬜ |

> **Замечание о сверке по эталону.** Где доступны независимые проверки (ручной/Excel-
> расчёт) — числа выводятся аналитически и закрепляются тестом. Где правило неоднозначно
> и требует именно выходов оригинала — это помечается в шаге и golden-снимок фиксирует
> текущее поведение до получения эталона, чтобы изменения были видимыми.

---

## Фаза B — Расширение функционала ядра

После стабилизации точности — наполнение оставшихся подмоделей (каждая со своими
тестами и сохранением инварианта):

- **Лизинг** (C25, имущество в лизинге B19, выкуп, страхование).
- **Краткосрочные инвестиции/депозиты** (C8/C9, B6/B18) и автоподбор профицита.
- **Активы:** переоценка (B9/B31), продажа ОС (C16, фин. результат), группировка ОС
  (земля/здания/оборудование вместо текущего обобщения в B14).
- **Налоги:** зарплатные взносы (база — ФОТ), налог с продаж (I3), пользовательские
  налоги (язык формул).
- **Две валюты и инфляция:** групповая индексация цен/издержек, пересчёт по `FX[t]`.
- **Детальный стартовый баланс** (≈20 статей вместо агрегатов).
- **Оценка бизнеса:** ✅ чистые активы (B33) и **модель Гордона** в `0.9.0`; впереди —
  ликвидационная стоимость, DDM, мультипликаторы (SPEC §20).

---

## Фаза C — Дополнительный UI

(после точности — как и договаривались: «потом можно будет сделать доп. UI»)

- **Холдинг / Integrator:** UI консолидации группы проектов.
- **Актуализация (план-факт):** ввод факта и просмотр отклонений в UI.
- **Результаты:** графики-долёвки (структура издержек/активов), отчёт об использовании
  прибыли и безубыточность в UI.
- **Экспорт:** PDF/XLSX отчётов; **импорт `.pex`** (миграция из оригинала).
- **Оценка бизнеса** и сравнение сценариев в интерфейсе.
- **UX-полировка:** валидация модели, подсказки, локализация (EN), доступность.

---

## Фаза D — Платформа и продакшен

- **Изоляция арендаторов:** Postgres Row-Level Security (сейчас — фильтрация по
  `organization_id` в запросах).
- **Фоновые задачи:** Celery/воркеры для тяжёлого анализа (Монте-Карло, What-If,
  чувствительность) и экспорта.
- **Наблюдаемость:** логи/метрики/трассировка; алерты на расхождение инварианта.
- **Безопасность и соответствие:** аудит, 152-ФЗ (хранение данных в РФ), 54-ФЗ (чеки),
  ставки лимитов, ротация секретов.
- **Биллинг в проде:** боевые вебхуки ЮKassa, проверка подписи, повторные попытки.
- **Нагрузочное и приёмочное тестирование** на отраслевых примерах.

---

## Вне паритета с Project Expert

Функции, которых у оригинала нет — начало продуктовой дифференциации (не копия PE,
а следующий шаг).

- ✅ **Ф10 — Ревью бизнес-плана** («линтер модели»): детерминированный этап после
  расчёта, который выдаёт находки, рекомендации и «светофор» по пяти категориям —
  жизнеспособность, ликвидность, структура, допущения и **дивергенция** (план ↔
  вероятное будущее через Монте-Карло и чувствительность). Без ИИ, чистые функции
  над результатом расчёта (`calc_core/review/`), golden движка не затрагивает.
  **Гейт финализации** (решение Q4): план нельзя финализировать, пока risk-находки не
  подтверждены осознанно; warning/info не блокируют. Снимок ревью и отпечаток модели
  хранятся для детекта дрейфа. API `GET /projects/{id}/review`,
  `POST /projects/{id}/finalize`; UI — вкладка «Ревью плана». Методика и решения —
  `docs/PLAN-REVIEW-ADVISOR.md`, `docs/REVIEW-ADVISOR-DECOMPOSITION.md`.
- **Впереди (v2+):** пер-продуктовая маржа (нужна спецификация BOM), пользовательские
  ожидания-бенчмарки (относительные → внешние ориентиры), настройка порогов ревью
  на уровне организации.

---

## Ближайшие шаги (детализация)

1. ✅ **A1 — график инвестиций (NPV/PI)** — сделано в `0.7.0`. `Inv[t]` = прирост
   накопленного дефицита относительно максимума предыдущих периодов; `PI = 1 + NPV/PV_инв`;
   добавлены `pv_investments` и `peak_financing_need`. NPV не изменился (не зависит от
   разбиения), PI пересчитан с базой «потребность в капитале».
2. ✅ **A2 — проводка I24** — сделано в `0.7.1`. Невычитаемые издержки (общие «из прибыли»
   и проценты «на прибыль») не входят в налоговую базу `I26`, уменьшают чистую прибыль
   `I28 = I23 + I25 − I24 − I27`, реально оплачиваются — баланс сходится. Backward-
   compatible (с `I24 = 0` формулы прежние).
3. ✅ **A3 — НДС «по оплате»** — сделано в `0.7.2`. Признание НДС по факту денег
   (`vat_basis = payment`): отложенный исходящий НДС → `B21`, входной вне зачёта → `B7`;
   баланс сходится. Backward-compatible (по умолчанию «по отгрузке»). Property-тест на
   50 случайных моделях теперь покрывает обе базы НДС + флаги `from_profit` /
   `interest_on_profit`.
4. ✅ **A7 — перенос убытков и льготы** — сделано в `0.7.3`. Убыток периода
   накапливается в пуле и уменьшает базу будущих прибыльных периодов (`I22`); льгота
   `profit_tax_benefit_share` освобождает долю базы. Backward-compatible (без убытков —
   как прежде). Property-тест расширен льготой.
5. ✅ **A6 — коэффициенты** — сделано в `0.7.4`. Оборачиваемость и ROA/ROE считаются на
   **средние за период** величины (начало t=0 = стартовый баланс через `opening_balance`);
   ликвидность, структура и «на акцию» — на конец периода. Изменились только коэффициенты
   (отчёты и показатели не тронуты).
6. ✅ **A5 — автоподбор** — сделано в `0.7.5`. Привлечение покрывает накопленные проценты;
   внешняя итерация по налоговой обратной связи — с адаптивным демпфированием (сходится
   даже при 200% годовых). Демпфирование не меняет результат — только путь к нему.
7. ✅ **A8 — запасы ГП** — сделано в `0.7.6`. Метод оценки себестоимости
   `inventory_method`: средняя (пул) либо ФИФО (по партиям выпуска); оба сохраняют
   тождество запаса → баланс сходится. НЗП (B4) длительного цикла — в Фазе B.

**Итог фазы A (точность).** Реализованы механизмы по **всем 8** пунктам §22
(`0.7.0…0.8.0`); каждый backward-compatible, с аналитическими тестами и видимым
golden-диффом. Фиксация `engine_version 1.0` — после сверки точных конвенций по эталону
(A10) и расширения golden-набора (A9).

**Фаза B — расширение ядра (текущая).** Начата с валютного контура:
8. ✅ **B1 — валютный контур + курсовая разница (A4/I25)** — `0.8.0`. Курс `fx_rate[t]`/
   `fx_open`; переоценка монетарной позиции `foreign_monetary` → `I25` (доход при росте
   курса) и `B6`; баланс сходится, property-тест гоняет валютную позицию на 50 моделях.
9. ✅ **B2 — валютные займы** — `0.8.1`. `Loan.foreign`: график в валюте займа,
   пересчёт по `FX[t]`; долг переоценивается → курсовой убыток в `I25` при росте курса.
   При `FX≡1` тождественно обычному займу.
10. ✅ **B3 — валютная выручка и издержки** — `0.8.2`/`0.8.3`. `SalesLine.foreign`
    (экспорт: выручка по курсу отгрузки, дебиторка переоценивается) и
    `FixedCostLine.foreign` (валютная услуга: кредиторка переоценивается) → `I25`.
    Валютный контур по выручке/услугам закрыт; валютное сырьё — далее (п. 18).
11. ✅ **Страховые взносы с ФОТ** — `0.8.4` (`payroll_contribution_rate`): загружают
    затраты на персонал, вычитаемая издержка.
12. ✅ **Продажа ОС** — `0.8.5` (`Asset.sale_month`/`sale_price`): амортизация
    прекращается, остаточная выбывает, поступления `C16`, фин. результат → `I20`/`I21`.
13. ✅ **Инфляция** — `0.8.6` (`inflation_sales/direct/wages/general`): индексация
    введённых сумм накопленным индексом по группам (период 0 — база).
14. ✅ **Лизинг + депозиты** — `0.8.7`. Операционный лизинг (`Financing.leases`: издержка
    I21 + отток C25) и депозиты/ЦБ (`Financing.deposits`: вложение C8, доход C9, тело B6).
15. ✅ **Налог с продаж** (I3) — `0.8.8` (`sales_tax_rate`, база — I1, не НДС).
16. ✅ **Переоценка ОС** — `0.8.9` (`Asset.revaluation_month`/`amount`): меняет `B9`
    и добавочный капитал `B31`, дальнейшая амортизация — от новой стоимости.
17. ✅ **Оценка бизнеса** — `0.9.0` (`reports/valuation.py`, поле `valuation`): чистые
    активы (B33) и модель Гордона на нормализованном годовом потоке; `g` —
    `terminal_growth_rate`. UI-карточка на странице результатов.
18. ✅ **Валютное сырьё** — `0.9.1` (`DirectCostLine.foreign`): запас `B3`/себестоимость —
    по курсу закупки (немонетарный актив), кредиторка переоценивается → `I25`; импортный
    НДС добавлен в `0.9.8`. Валютный контур закрыт по всем основным статьям.
19. ✅ **НЗП** — `0.9.2` (`production_cycle_months`): производственный цикл сдвигает выпуск
    и его стоимость на `cycle` мес.; «в пути» стоимость копится в `B4 = cum(запуск) −
    cum(выпуск)`. Денежные потоки/расход сырья — в периоде запуска; при `cycle=0` `B4≡0`.
20. ✅ **Группировка ОС** — `0.9.3` (`Asset.category`): остаточная стоимость по группам —
    земля `B12` / здания `B13` / оборудование `B14`. Земля не амортизируется и вне базы
    налога на имущество (база = `B13+B14`); по умолчанию — оборудование (golden без дрейфа).
21. ✅ **Финансовый лизинг** — `0.9.4` (`Lease.finance`): предмет капитализируется (B19,
    амортизация I17), обязательство (→ B26) гасится телом, проценты — I18; платёж в C25.
    По умолчанию лизинг операционный (без дрейфа).
22. ✅ **Методы оценки бизнеса** — `0.9.5`: **DDM** (капитализация дивидендов C26) и
    **мультипликатор прибыли** (`valuation_earnings_multiple` × годовая I28) добавлены к
    чистым активам и Гордону; аддитивно (golden — версия + новые поля оценки).
23. ✅ **Стартовый оборотный капитал** — `0.9.6` (`StartingBalance.receivables/payables`):
    стартовая дебиторка (B2) и кредиторка (B23) действующего предприятия входят в проверку
    сходимости и разворачиваются в денежный поток в первом месяце (инкассация C1/оплата C2).
24. ✅ **Ликвидационная стоимость** — `0.9.7` (`liquidation_recovery_rate`): пятый метод
    оценки — `доля_возврата · активы (B20) − обязательства`; аддитивно (golden — версия + поле).
25. ✅ **Импортный НДС** — `0.9.8` (`DirectCostLine.foreign` + НДС): начисляется на
    таможенную стоимость, к вычету, уплата на таможне при ввозе (`C2`).
26. ✅ **Стартовые запасы** — `0.9.9` (`StartingBalance.raw_materials/finished_goods`):
    запасы сырья `B3`/ГП `B5` действующего предприятия как поддерживаемый уровень (постоянны,
    входят в проверку сходимости); golden без дрейфа (по умолчанию 0).
27. ✅ **Отраслевые golden-сценарии** — `0.9.9` (A9): добавлены `trade` (опт) и `services`
    (консалтинг) к производственным сценариям — приёмочный набор по типам бизнеса.
28. ✅ **Стартовый краткосрочный долг** — `0.9.10` (`StartingBalance.short_term_debt`):
    краткосрочные займы действующего предприятия → `B22`; несутся постоянно (как долгосрочный
    долг в `B26`, авто-погашения нет), входят в проверку сходимости и в краткосрочные
    обязательства `B25`; golden без дрейфа чисел (по умолчанию 0, только версия). UI —
    вкладка «Валюта и старт».
29. ✅ **Стартовая структура капитала** — `0.9.11` (`StartingBalance.preferred_capital/`
    `reserves/additional_capital`): привилегированные акции `B28`, резервные фонды `B30`,
    добавочный капитал `B31` (+ дооценка ОС) — несутся постоянно, входят в собственный
    капитал `B33` и в проверку сходимости; golden без дрейфа чисел. UI — вкладка «Валюта и
    старт».
30. ✅ **Стартовые авансы** — `0.9.12` (`StartingBalance.prepaid_expenses/advances_received`):
    выданные авансы/предоплата → `B7` (актив), полученные авансы → `B24` (пассив);
    поддерживаемый уровень (как запасы: постоянны, денежного потока не создают), аддитивны к
    динамике (НДС-кредит `B7`, предоплата по плану продаж `B24`), входят в проверку
    сходимости; golden без дрейфа чисел. **Детальный стартовый баланс закрыт** (оборотный
    капитал + структура капитала). UI — вкладка «Валюта и старт».
31. ✅ **Страхование лизинга** — `0.9.13` (`Lease.insurance_monthly`): помесячное страхование
    предмета лизинга — операционная издержка `I21` и отток `C25` за срок; применяется к обоим
    типам (операционному и финансовому); аддитивно, golden без дрейфа чисел. UI — вкладка
    «Финансирование».
32. ✅ **Выкуп лизинга** — `0.9.14` (`Lease.buyout_price/buyout_life_months`): по окончании
    лизинга (мес. `start+term`) предмет выкупается и становится собственным ОС — через тот же
    механизм, что и плановые активы (приобретение `C14`, амортизация `I17`/`B10`, остаточная
    → `B14`). Работает для обоих типов лизинга; аддитивно, golden без дрейфа чисел. UI —
    вкладка «Финансирование».
33. ✅ **Полное покрытие инварианта** — property-тест (`test_invariants.py`, 50 моделей)
    рандомизирует **весь** функционал: детальный стартовый баланс (оборотный капитал +
    структура капитала), лизинг со страхованием и выкупом, валюта, налоги, инфляция, займы,
    депозиты, автоподбор. Стартовый баланс замыкается конструктивно; `B20=B34`, `B1=C29`,
    `B32=P7` держатся во всех периодах. Готовность к сверке по эталону.
34. ✅ **Календарный план и бюджетирование по этапам** — `0.9.15…0.9.19` (аналог
    «Инвестиционного плана» PE; `models/calendar.py`, `engine/calendar.py`). Этапы
    подготовительной фазы: тип (издержки/актив/производство), сроки, связи-предшественники
    (финиш→старт), иерархия групп, стоимость (прямая или Σ ресурсов с задержкой оплаты).
    Трактовка: обычный этап → издержки подготовительного периода `C15` (сразу `I21` либо РБП
    `B15` со списанием); этап-актив → ОС (реюз машинерии: `C14`/`I17`/`B14`); этап
    «производство» → старт продукта (`SalesLine.start_month`). Тайминг `uniform`/`on_finish`.
    **Бюджетный выход** (`CalcResult.budget`): смета по этапам (свёртка групп) + помесячный
    график инвестиций. API `GET /projects/{id}/budget`; UI — вкладка «Календарный план»
    (карточки этапов, диаграмма Гантта, ресурсы, живая смета). Golden без дрейфа чисел
    (пустой план инертен). План — `docs/CALENDAR-PLAN-DECOMPOSITION.md`.
35. ✅ **Пер-продуктная экономика (BOM)** — `0.9.20…0.9.21` (пакет №1 gap-анализа;
    `Product.bom`/`piece_wage_per_unit`, справочник `OperatingPlan.materials`). Рецептура
    разворачивается в синтетические прямые издержки (реюз машинерии: деньги, запас `B3`,
    кредиторка `B23`, НДС/импорт, инфляция); маржа по продуктам —
    `CalcResult.product_margins` (без фейковой аллокации суммовых статей); пер-продуктные
    правила ревью (`product_negative_margin`/`product_thin_margin` — v2 ревью закрыт). UI:
    рецептура и материалы во вкладке «Сбыт», таблица маржи в «Сводке». Golden без дрейфа
    (модель без рецептур инертна). План — `docs/PRODUCT-ECONOMICS-DECOMPOSITION.md`.
36. ✅ **Мелкие дыры ядра (пакет №2 gap-анализа)** — `0.9.22…0.9.25`: прочие
    поступления/выплаты (`OtherFlow` → I20/C10, I21|I24/C11); **MIRR** и **ARR** (полный
    набор показателей PE); сложные схемы оплаты продаж (`PaymentTerms.schedule` — график
    долей со сдвигами, несколько авансов/рассрочек); пер-строчная ставка НДС
    (`SalesLine.vat_rate` — 10%/20%/0% в одном проекте). Всё backward-compatible, каждое —
    UI + тесты + property-инвариант.
37. ✅ **План персонала (пакет №3 gap-анализа)** — `0.9.26` (`StaffPosition` →
    `OperatingPlan.staff`): должность × оклад × численность на период `[start, end)` с
    функцией разноски (адм./произв./маркетинг) и задержкой выплаты. Разворачивается в
    синтетические статьи персонала — та же машинерия (взносы с ФОТ, индексация инфляцией
    зарплаты, деньги C6, кредиторка B23). UI — секция «План персонала» с ФОТ на вкладке
    «Издержки». Пустой штат инертен → golden без дрейфа.
38. ✅ **Язык формул и таблицы пользователя (пакет №4 gap-анализа)** — `0.9.27`
    (`calc_core/formula` + `ProjectModel.user_tables`): безопасный парсер/вычислитель на
    Decimal (без eval, DoS-лимиты), 18 функций с русскими алиасами, идентификаторы — коды
    строк отчётов + N. Таблицы считаются после отчётов (методика не меняется), ошибки формул
    не роняют расчёт. UI: вкладка «Таблицы» в редакторе (формулы + справка) и в результатах
    (значения, ошибки). Пустые таблицы инертны → golden без дрейфа. План —
    `docs/FORMULA-TABLES-DECOMPOSITION.md`.
39. ✅ **DOCX-бизнес-план + экспертное заключение (пакет №5 gap-анализа)** —
    `calc_core/review/opinion.py` (детерминированный автотекст заключения из ревью и
    показателей; поле `opinion` в `/review` и в снимке финализации) +
    `ProjectModel.business_plan` (текстовые разделы) + `app/docgen.py` (python-docx:
    титул → заключение → показатели → разделы → 4 отчёта → маржа/смета; годовая свёртка
    отчётов при n > 24 на слое генерации) + `GET /projects/{id}/business-plan.docx`.
    UI: вкладка «Документ» в редакторе, кнопка «Бизнес-план» на результатах, карточка
    заключения в ревью. Движок не тронут → golden без дрейфа. План —
    `docs/BUSINESS-PLAN-DOC-DECOMPOSITION.md`.
40. ✅ **Агрегация квартал/год + детализация строк (пакет №6 gap-анализа)** —
    свёртка отчётов на слое отображения (`frontend/src/aggregate.ts`, переключатель
    «Месяц | Квартал | Год», дефолт по горизонту; правила DOCX-генератора) +
    `CalcResult.details`: слагаемые ключевых строк, сохранённые конвейером
    (I1/C1 по продуктам, C2/C3/I16 по статьям, C14 по активам; Σ = строка отчёта
    точно, в golden-снимок не входит) с раскрытием строк в таблицах результатов.
    Ядро помесячно, методика не менялась. План —
    `docs/AGGREGATION-DRILLDOWN-DECOMPOSITION.md`.
41. ✅ **Настраиваемые налоги (gap 2.1)** — `0.9.28`: `Environment.taxes` →
    движок произвольных налогов (базы revenue/ФОТ/имущество/прибыль/формула — по
    предварительному прогону; отнесение I21/I24; периодичность месяц/квартал/год →
    C12/B21; ошибка формулы базы отклоняет расчёт; детализация C12). UI — секция
    «Настраиваемые налоги» на вкладке «Проект». Пустой список инертен (golden —
    только строка версии). План — `docs/CUSTOM-TAXES-DECOMPOSITION.md`.
42. ✅ **Доходы участников (пакет №7 gap-анализа)** — `0.9.29`:
    `CalcResult.participants` — потоки и NPV/IRR акционеров (−C21+C26, equity-IRR
    с терминальной стоимостью B33) и кредиторов по займам (график движка, FX для
    валютных, терминальная — непогашенное тело; IRR полного займа = ставке).
    Сводка результатов + DOCX. План — `docs/PARTICIPANTS-DECOMPOSITION.md`.
43. ✅ **Версии проекта и анализ изменений (пакет №8, gap 4.4)** — таблица
    `project_versions` (снимки модели, RLS-изоляция) + API (снимок/список/диф/
    восстановление/удаление) + вкладка «Версии» на странице анализа. Диф — по
    листовым путям модели (added/removed/changed) и заголовочным показателям
    (версия ↔ версия либо ↔ текущая рабочая модель). Ядро/golden не затронуты
    (только чтение `run()`). План — `docs/PROJECT-VERSIONS-DECOMPOSITION.md`.
44. ✅ **Автоподбор инвестиций (пакет №8, gap §6)** — `0.9.30`: `invest_surplus`
    размещает излишки кассы в депозит симметрично автокредиту (единый решатель
    `solve_cash_management`; equivalence с `solve_credit_line` при выключенном
    размещении → кредит-only без дрейфа). Доход депозита растит налог, итерация по
    двум рядам. UI — секция «Автофинансирование». План — `docs/AUTO-INVEST-DECOMPOSITION.md`.
45. ✅ **Нормирование процентов по ставке ЦБ (пакет №8, gap 2.2)** — `0.9.31`:
    `cb_refinancing_rate` × `interest_norm_multiple` задают предел вычитаемых процентов;
    `_loans` расщепляет процент вычитаемого займа на I18 (в пределах) и I24 (сверх).
    Ставка ЦБ = 0 → выключено (golden = строка версии). UI — секция «Налоги».
    План — `docs/INTEREST-NORM-DECOMPOSITION.md`.
46. ✅ **Доинвестирование в актив (пакет №8, gap 1.6)** — `0.9.32`:
    `Asset.additional_investments` (модернизация); `_assets` переписан на слоистую
    амортизацию — база + слои доп. вложений, амортизируемых от остаточного срока.
    Актив без доинвестиций = прежние числа (golden = строка версии). UI — блок в
    карточке актива. План — `docs/ADDITIONAL-INVESTMENT-DECOMPOSITION.md`.
47. ✅ **Погодовые ряды инфляции (пакет №8, gap 1.9)** — `0.9.33`:
    `inflation_*_series` (ставки по годам) переопределяют константы; `_inflation_index`
    по годам (постоянный ряд ≡ скаляр → golden = строка версии). UI — таблица
    «Инфляция по годам». Инфляция ОС/capex — v2. План —
    `docs/INFLATION-SERIES-DECOMPOSITION.md`.
48. **Впереди:** сверка методики по эталону (A10, нужны эталонные расчёты PE 7.21) → фиксация
    `1.0`; остаток пакета №8 (⚪ низкий приоритет): актуализация этапов (4.6),
    НМА/детализация стартовых ОС (1.8, 1.7), инфляция ОС/capex (1.9 v2).

Каждый шаг: реализация → аналитический тест → перегенерация golden (`UPDATE_GOLDEN=1
pytest`) → ревью диффа чисел → коммит с пометкой влияния на `engine_version`.

---

*План отражает текущее понимание и уточняется по мере сверки методики. Реализация —
стандартная финансовая методология; проприетарный код Project Expert не используется.*
